import { test, expect } from '@playwright/test';

test.describe('EPAM Services -> Client Work navigation', () => {
  test('should open Client Work page from Services menu', async ({ page }) => {
    // Test steps will be added in the next update.
  });
});
